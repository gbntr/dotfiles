input {
    keyboard {
        xkb {
            // layout "us,ru"
        }
        numlock
    }

    touchpad {
        tap
        dwt
        natural-scroll
        accel-speed 0.2
    }
}

layout {
    background-color "#00000000"
    
    // Gaps (Espaçamento)
    gaps 8

    center-focused-column "never"

    preset-column-widths {
        proportion 0.333
        proportion 0.5
        proportion 0.667
        proportion 1.0
    }

    // 2 janelas dividem a tela.
    default-column-width { proportion 1.0; }

    // Focus Ring (Desligado)
    focus-ring {
        off
        width 2
        active-color "#003652ff"
        inactive-color "#313244"
    }

    // Border (Sua cor Azul)
    border {
        off
        width 2
        active-color "#003652ff" 
        inactive-color "#313244" 
        urgent-color "#ff0000"
    }

    shadow {
        on
        softness 30
        spread 5
        offset x=0 y=5
        color "#0007"
    }
    
    struts {}
}

overview {
    backdrop-color "#000001"
}

environment {
    ELECTRON_OZONE_PLATFORM_HINT "auto"
    //DISPLAY ":0"
    XCURSOR_SIZE "20"
    XCURSOR_THEME "Bibata-Modern-Ice"

    // Força Thunar e apps GTK a não desenharem a barra de título
    GTK_CSD "0"
    GTK_DECORATION_LAYOUT ":"
    QT_QPA_PLATFORM "wayland;xcb"
    QT_WAYLAND_DISABLE_WINDOWDECORATION "1"
}

hotkey-overlay {
    skip-at-startup
}

cursor {
    xcursor-theme "Bibata-Modern-Ice"
    xcursor-size 20
}

// --- INICIALIZAÇÃO (STARTUP) ---

spawn-at-startup "easyeffects" "--gapplication-service"
spawn-at-startup "xwayland-satellite"
spawn-at-startup "/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1"
spawn-at-startup "dbus-update-activation-environment" "--systemd" "WAYLAND_DISPLAY" "XDG_CURRENT_DESKTOP"
spawn-at-startup "gnome-keyring-daemon" "--start" "--components=secrets,pkcs11,ssh"

// Substituição Waybar/SwayNC por Noctalia
spawn-at-startup "noctalia"

spawn-at-startup "swww-daemon"
spawn-at-startup "swww" "img" "/home/guilherme/Pictures/wallpapers/blueshade.png" //"--resize-type" "stretch"
spawn-at-startup "motrix"

screenshot-path "~/Pictures/Screenshots/Screenshot from %Y-%m-%d %H-%M-%S.png"

prefer-no-csd

animations {
    slowdown 1.0
}

// --- REGRAS DE JANELA (WINDOW RULES) ---

layer-rule {
    // Tente encontrar o namespace correto. Geralmente é "swww-daemon" ou "swww".
    // Você pode confirmar rodando "niri msg layers" no terminal.
    match namespace="^swww-daemon$" 
    
    // Esta é a mágica: coloca o layer dentro do backdrop do overview
    place-within-backdrop true
}

window-rule {
    match app-id="imv"
    open-floating true
    default-column-width {proportion 0.5;}
}

window-rule {
    opacity 0.95
    match is-active=true
}

window-rule {
    opacity 0.90
    match is-active=false
}

window-rule{
    geometry-corner-radius 12
    clip-to-geometry true
}


// FIX ZEN BROWSER: Garante que ele ocupe 100% da coluna e não flutue
window-rule {
    match app-id="zen"
    match app-id="zen-alpha"
    match app-id="navigator"

    open-floating false
    default-column-width { proportion 1.0; }
}

window-rule {
    match app-id="Motrix"
    open-floating true
}

// WezTerm Fix
window-rule {
    match app-id=r#"^org\.wezfurlong\.wezterm$"#
    default-column-width {}
}

// Firefox PiP
window-rule {
    match app-id=r#"firefox$"# title="^Picture-in-Picture$"
    open-floating true
}


// --- CORREÇÃO: JANELAS FLUTUANTES (LINHAS SEPARADAS) ---
window-rule {
    // Thunar e Gerenciamento de Arquivos
    match title=r#"Renomear"#
    match title=r#"Rename"#
    match title=r#"Confirmar"#
    match title=r#"Confirm"#
    match title=r#"Progresso"#
    match title=r#"Progress"#
    match title=r#"Propriedades"#
    match title=r#"Properties"#
    match title=r#"Copy"#
    match title=r#"Copiar"#
    match title=r#"Copiando"#
    
    // Portals (Salvar/Abrir do VSCode, Navegadores, etc)
    match title=r#"Abrir"#
    match title=r#"Open"#
    match title=r#"Salvar"#
    match title=r#"Save"#
    match app-id=r#"xdg-desktop-portal-gtk"#
    match app-id=r#"xdg-desktop-portal-gnome"#
    match app-id=r#"polkit-gnome-authentication-agent-1"#
    
    open-floating true
    default-column-width {} 
}



// --- ATALHOS (BINDS) ---
binds {  
    Mod+Shift+Slash { show-hotkey-overlay; }

    // Apps Básicos
    Mod+Return hotkey-overlay-title="Terminal" { spawn "kitty"; }
    Mod+Shift+Return {
        spawn "kitty" "--class" "floating" "-o" "remember_window_size=no" "-o" "initial_window_width=800" "-o" "initial_window_height=500";
    }
    Mod+D { spawn "rofi" "-show" "drun" "-theme" "~/.config/rofi/launchers/type-3/style-3.rasi"; }

    // SEUS ATALHOS PERSONALIZADOS
    Mod+E hotkey-overlay-title="Thunar" { spawn "thunar"; }
    Mod+B hotkey-overlay-title="Browser" { spawn "zen-browser"; }
    Mod+Alt+L hotkey-overlay-title="Lock" { spawn "swaylock"; }
    Mod+Alt+P hotkey-overlay-title="Power Off" { spawn "systemctl" "poweroff"; }

    Mod+Space { toggle-window-floating; }

    // Audio
    XF86AudioRaiseVolume allow-when-locked=true { spawn "wpctl" "set-volume" "@DEFAULT_AUDIO_SINK@" "0.05+"; }
    XF86AudioLowerVolume allow-when-locked=true { spawn "wpctl" "set-volume" "@DEFAULT_AUDIO_SINK@" "0.05-"; }
    XF86AudioMute        allow-when-locked=true { spawn "wpctl" "set-mute" "@DEFAULT_AUDIO_SINK@" "toggle"; }
    XF86AudioMicMute     allow-when-locked=true { spawn "wpctl" "set-mute" "@DEFAULT_AUDIO_SOURCE@" "toggle"; }

    // --- BRILHO (Brightness) ---
    XF86MonBrightnessUp allow-when-locked=true { spawn "brightnessctl" "set" "+5%"; }
    XF86MonBrightnessDown allow-when-locked=true { spawn "brightnessctl" "set" "5%-"; }

    Mod+O repeat=false { toggle-overview; }

    Mod+Q { close-window; }
    Mod+Shift+Q { spawn "bash" "-c" "niri msg focused-window | jq '.pid' | xargs kill -9"; }

    Mod+V { spawn "~/.config/niri/scripts/permanent-floating.sh"; }

    // Navegação
    Mod+Left  { focus-column-left; }
    Mod+Alt+Down  { focus-window-down; }
    Mod+Alt+Up    { focus-window-up; }
    Mod+Right { focus-column-right; }

    Mod+Shift+Left  { move-column-left; }
    Mod+Alt+Shift+Down  { move-window-down; }
    Mod+Alt+Shift+Up    { move-window-up; }
    Mod+Shift+Right { move-column-right; }

    Alt+Left { focus-column-first; }
    Alt+Right  { focus-column-last; }
    Alt+Shift+Left { move-column-to-first; }
    Alt+Shift+Right  { move-column-to-last; }

    // Monitores
    Mod+Ctrl+Left  { focus-monitor-left; }
    Mod+Ctrl+Down  { focus-monitor-down; }
    Mod+Ctrl+Up    { focus-monitor-up; }
    Mod+Ctrl+Right { focus-monitor-right; }

    Mod+Shift+Ctrl+Left  { move-column-to-monitor-left; }
    Mod+Shift+Ctrl+Down  { move-column-to-monitor-down; }
    Mod+Shift+Ctrl+Up    { move-column-to-monitor-up; }
    Mod+Shift+Ctrl+Right { move-column-to-monitor-right; }
    Mod+Shift+Ctrl+H     { move-column-to-monitor-left; }
    Mod+Shift+Ctrl+J     { move-column-to-monitor-down; }
    Mod+Shift+Ctrl+K     { move-column-to-monitor-up; }
    Mod+Shift+Ctrl+L     { move-column-to-monitor-right; }

    // Workspaces
    Mod+Page_Down       { focus-workspace-down; }
    Mod+Page_Up         { focus-workspace-up; }
    Mod+Down            { focus-workspace-down; }
    Mod+Up              { focus-workspace-up; }
    
    Mod+Shift+Down { move-column-to-workspace-down; }
    Mod+Shift+Up   { move-column-to-workspace-up; }
    Mod+Ctrl+U     { move-column-to-workspace-down; }
    Mod+Ctrl+I     { move-column-to-workspace-up; }

    Mod+Shift+Page_Down { move-workspace-down; }
    Mod+Shift+Page_Up   { move-workspace-up; }
    Mod+Shift+U         { move-workspace-down; }
    Mod+Shift+I         { move-workspace-up; }

    // Mouse Wheel
    Mod+WheelScrollDown     cooldown-ms=150 { focus-workspace-down; }
    Mod+WheelScrollUp       cooldown-ms=150 { focus-workspace-up; }
    Mod+Ctrl+WheelScrollDown cooldown-ms=150 { move-column-to-workspace-down; }
    Mod+Ctrl+WheelScrollUp   cooldown-ms=150 { move-column-to-workspace-up; }

    Mod+WheelScrollRight      { focus-column-right; }
    Mod+WheelScrollLeft       { focus-column-left; }
    Mod+Ctrl+WheelScrollRight { move-column-right; }
    Mod+Ctrl+WheelScrollLeft  { move-column-left; }

    Mod+Shift+WheelScrollDown      { focus-column-right; }
    Mod+Shift+WheelScrollUp        { focus-column-left; }
    Mod+Ctrl+Shift+WheelScrollDown { move-column-right; }
    Mod+Ctrl+Shift+WheelScrollUp   { move-column-left; }

    // Teclas Numéricas
    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }
    Mod+Ctrl+1 { move-column-to-workspace 1; }
    Mod+Ctrl+2 { move-column-to-workspace 2; }
    Mod+Ctrl+3 { move-column-to-workspace 3; }
    Mod+Ctrl+4 { move-column-to-workspace 4; }
    Mod+Ctrl+5 { move-column-to-workspace 5; }
    Mod+Ctrl+6 { move-column-to-workspace 6; }
    Mod+Ctrl+7 { move-column-to-workspace 7; }
    Mod+Ctrl+8 { move-column-to-workspace 8; }
    Mod+Ctrl+9 { move-column-to-workspace 9; }

    // Manipulação de Colunas
    Mod+BracketLeft  { consume-or-expel-window-left; }
    Mod+BracketRight { consume-or-expel-window-right; }
    Mod+Comma  { consume-window-into-column; }
    Mod+Period { expel-window-from-column; }

    Mod+R { switch-preset-column-width; }
    Mod+Shift+R { switch-preset-window-height; }
    Mod+Ctrl+R { reset-window-height; }
    Mod+F { maximize-column; }
    Mod+Shift+F { fullscreen-window; }
    Mod+Ctrl+F { expand-column-to-available-width; }

    Mod+C { center-column; }
    Mod+Ctrl+C { center-visible-columns; }

    // Resize
    Mod+Minus { set-column-width "-10%"; }
    Mod+Equal { set-column-width "+10%"; }
    Mod+Shift+Minus { set-window-height "-10%"; }
    Mod+Shift+Equal { set-window-height "+10%"; }

    // Floating
    Mod+Shift+V { switch-focus-between-floating-and-tiling; }

    Mod+W { toggle-column-tabbed-display; }

    // SCREENSHOTS (SEU PEDIDO: Mod+S)
    Mod+S { screenshot; }              // Interativo
    Mod+Ctrl+S { screenshot-screen; }  // Tela cheia
    Mod+Alt+S { screenshot-window; }   // Janela

    Mod+Escape allow-inhibiting=false { toggle-keyboard-shortcuts-inhibit; }
    Mod+Shift+P { quit; }
    Ctrl+Alt+Delete { quit; }
    Mod+Shift+E { power-off-monitors; }
}


window-rule {
    match app-id="org.qbittorrent.qBittorrent"
    open-floating true
}

